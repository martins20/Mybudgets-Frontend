export default interface IBudgetDTO {
  budget_id: string;
  name: string;
  dev_quantity: number;
  designer_quantity: number;
  po_quantity: number;
  sm_quantity: number;
  min_days: number;
  budget_price: number;
}
